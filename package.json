{
  "name": "empiretv-chat-backend",
  "version": "3.0.0",
  "description": "Empire TV — real-time chat backend (WebSocket + HTTP fallback + planilha archive)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ws": "^8.18.0"
  },
  "engines": { "node": ">=18" }
}
